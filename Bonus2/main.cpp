#include <iostream>
#include <math.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

using namespace std;

int main() {

float variabel1, summa;

cout << "Skriv in 5 heltal: ";
cin >> variabel1;
summa = variabel1 + summa;

cin >> variabel1;
summa = variabel1 + summa;

cin >> variabel1;
summa = variabel1 + summa;

cin >>variabel1;
summa = variabel1 + summa;

cin >> variabel1;
summa = variabel1 + summa;

summa = summa / 5;

cout << "Medeltalet = " << summa;
	return 0;
}
